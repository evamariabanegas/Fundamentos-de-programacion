#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<float> v = {3.5, 7.2, 1.8, 9.6, 4.1};
    float minimo = v[0];
    for (float x : v)
        if (x < minimo) minimo = x;
    cout << "El valor mínimo es: " << minimo << endl;
    return 0;
}
